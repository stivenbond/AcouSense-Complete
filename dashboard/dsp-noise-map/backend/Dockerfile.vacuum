FROM python:3.12-slim

RUN apt-get update && apt-get install -y \
    sqlite3 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY src/acousense/maintenance/vacuum.py /app/vacuum.py

ENTRYPOINT ["python", "/app/vacuum.py"]
